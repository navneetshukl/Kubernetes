apiVersion: v1
kind: Service
metadata: 
  name: task-api-service
  #namespace: task-system
spec:
  type: ClusterIP
  selector:
    app: task-api-pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080